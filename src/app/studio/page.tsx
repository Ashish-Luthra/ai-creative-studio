import { StudioHomeScreen } from '@studio/components/home/StudioHomeScreen'

/**
 * The Creative Studio's agent-first front door: a prompt box, recent work and
 * the capability cards. Surfaces live under /studio/<mode>.
 */
export default function StudioIndexPage() {
  return <StudioHomeScreen />
}
